const { oneA1, oneA2 } = require('./file-one-A')

describe('oneA1', () => {
	it('is defined', () => {
		expect(oneA1).toBeDefined()
	})
})

describe('oneA2', () => {
	it('is defined', () => {
		expect(oneA2).toBeDefined()
	})
})
